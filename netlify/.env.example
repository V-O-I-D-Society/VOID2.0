# Netlify environment (Site configuration -> Environment variables)
# Used at runtime by netlify/functions/api.js to forward /api/* to your backend.
# Only ONE variable is needed on Netlify. No trailing slash, no /api.

# Copy your real backend public URL here when deploying, e.g.:
BACKEND_URL=https://your-backend-service.up.railway.app
